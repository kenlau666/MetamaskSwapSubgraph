{
  "name": "metamaskSwapSubgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ kenlau/metamaskSwapSubgraph",
    "create-local": "graph create --node http://localhost:8020/ kenlau/metamaskSwapSubgraph",
    "remove-local": "graph remove --node http://localhost:8020/ kenlau/metamaskSwapSubgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 kenlau/metamaskSwapSubgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.58.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
